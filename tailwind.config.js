/** @type {import('tailwindcss').Config} */
export default {
    content: [
        './index.html',
        './src/**/*.{js,ts,jsx,tsx}', // Include JS, JSX, TS, and TSX files in the src folder
    ],

    theme: {
        extend: {},
    },
    plugins: [],
};